# AGENTS.md — sorcerics (SOL 웹사이트)

모든 코딩 에이전트(Claude Code, OpenAI Codex, Cursor 등)가 매 세션 읽는 공통 규칙이다. Claude Code는 이 폴더와 상위에 `CLAUDE.md`가 없을 때 이 파일을 직접 읽으므로 `CLAUDE.md`를 따로 두지 않는다(만들면 이 파일이 무시된다). 아래 "세션 히스토리 기록" 규칙은 사용자가 요청하지 않아도 항상 따른다.

## 프로젝트 요점

- 정적 사이트(HTML/CSS/JS, 빌드 없음). 프로덕션은 **Vultr `sol-web`** http://158.247.214.3/ (nginx, `/var/www/html`). 배포는 `bash scripts/deploy.sh` (서버 백업 → rsync → 스모크 체크). GitHub 레포 `sangyongbot/sorcerics`는 소스 백업용이며 GitHub Pages는 쓰지 않는다.
- 레이아웃은 디자인 시안(`~/Downloads/new web ongoing.pdf`, 1366×768pt) 단위계 `--u = 100vw/1366` 기준. 자세한 규칙은 `docs/superpowers/specs/`와 메모리 참조.
- 고객 공유용 리포트는 `docs/report-<날짜>.md`, 세션 히스토리는 `docs/history/`.

## 세션 히스토리 기록 (필수)

**목적.** 차후 어떤 과정을 통해 작업이 진행됐는지 기억하고, 필요하면 작업 내역을 증빙할 수 있게 한다. 단순 로그가 아니라 사람이 읽고 재구성할 수 있는 기록이다.

**규칙.**

1. **세션 하나당 파일 하나.** 경로 `docs/history/session-YYYY-MM-DD-<topic>.md`. 날짜는 세션 시작일(KST), `<topic>`은 영문 소문자 kebab-case 2~4단어(예: `site-rebuild`, `mobile-audit`). 같은 날 세션이 여럿이면 topic으로 구분한다.
2. **세션 시작 때, 다른 작업보다 먼저 파일을 만든다.** `docs/history/TEMPLATE.md`를 복사해 메타 블록을 채우고 타임라인은 비워 둔다. 메타의 "도구"에 사용 중인 에이전트 이름을 적는다(Claude Code / Codex / …).
3. **작업 단위가 끝날 때마다 타임라인에 한 줄.** 커밋·배포·설계 결정·사용자 지적으로 방향이 바뀐 일·실수는 각각 한 항목. 개발과 무관한 질문/답변, 짧은 확인 핑퐁, 같은 주제의 잔손질 연속은 하나로 묶어 요약한다. 시각은 KST.
4. **세션 마무리(사용자가 끝내거나 정리를 요청할 때, 또는 큰 작업 덩어리가 끝났을 때)** 실수·상세·메모리·후속 작업 절을 채우고 메타의 결과·코드 변경을 확정한다.
5. **커밋에 포함한다.** 히스토리 파일만 바뀌어도 커밋한다. 다른 세션의 히스토리 파일은 수정하지 않는다(링크만 건다).
6. **돌아가기 링크.** 로컬 CLI 세션은 웹 URL이 없다. 대신 메타에 그 도구의 세션 ID와 재개 명령, 트랜스크립트 경로를 남긴다.
   - Claude Code: `claude --resume <세션 ID>`(이 프로젝트 폴더에서 실행), 트랜스크립트 `~/.claude/projects/-Users-sj-Projects-sorcerics/<세션 ID>.jsonl`. 세션 ID는 시스템 프롬프트의 스크래치패드 경로(`…/<세션 ID>/scratchpad`)에서 읽는다. 보관 기간(기본 30일, `cleanupPeriodDays`)이 지나면 트랜스크립트가 지워진다.
   - Codex CLI: `codex resume <세션 ID>`, 세션 기록은 `~/.codex/sessions/`. 다른 도구는 해당 도구의 재개 방법을 같은 형식으로 적는다.
   - 트랜스크립트가 사라진 뒤에는 히스토리 파일이 유일한 기록이므로, 파일만 읽어도 과정이 재구성되게 쓴다.

**파일 형식** (`docs/history/TEMPLATE.md` 그대로):

- 제목 `# 세션 히스토리 — <주제>`
- 메타: 날짜 · 도구 · 세션 ID · 돌아가기 · 시작 시점 저장소 상태 · 요청 · 결과 · 저장소 코드 변경
- `## 1. 타임라인` — 표 `# | 내용 | 결과` (결과 칸에 커밋 해시, 배포, "오판" 같은 판정)
- `## 2. 기록해 둘 실수` — 무엇을 잘못 판단했고 어떻게 바로잡았는지, 교훈 한 줄
- `## 3.` 이후 — 상세 절(필요한 만큼: 변경 상세, 커밋 목록, 조사 결과 등)
- `## n-1. 기록한 메모리` — 이 세션에서 쓴 메모리 파일과 요지
- `## n. 이 세션 범위 밖의 후속 작업` — 미결·대기 항목

기존 예시: `docs/history/session-2026-09-21-site-rebuild.md`.
